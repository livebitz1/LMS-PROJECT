// Centralized icons for Navbar using lucide-react (shadcn/ui)
import { Home, BookOpen, Users, MessageCircle, LayoutDashboard, CalendarCheck, GraduationCap } from "lucide-react";

export const NavbarIcons = {
  home: Home,
  courses: BookOpen,
  mentors: Users,
  messages: MessageCircle,
  teacherDashboard: LayoutDashboard,
  bookings: CalendarCheck,
  learnerDashboard: GraduationCap,
};
